# DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# For use in OpenAi Tracing
TRACE_NAME=Screen

# API keys
OPENAI_API_KEY=sk-key-goes-here
BRAVE_SEARCH_API_KEY=BS-key-goes-here

# Default is 10
MAX_TURNS=15

# Vector DB settings 
# only used if EMBEDDING_PROVIDER is set to openai
EMBEDDING_MODEL=text-embedding-3-large
# openai or builtin
EMBEDDING_PROVIDER=openai

# Codebase paths
# unpacks and executes ./setup.sh in sandbox
SETUP_ARCHIVE=/upload.tgz
# folder with to subfolders containing vulnerable and patched codebase
CODEBASE_PATH=/codebase
# folder name of the vulnerable codebase in CODEBASE_PATH
VULNERABLE_CODEBASE_FOLDER=vulnerable-version
# folder name of the patched codebase in CODEBASE_PATH
PATCHED_CODEBASE_FOLDER=patched-version
